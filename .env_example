# Copy this file to .env and adjust the values:
# cp .env.example .env

# Database (MySQL) — used by backend and db service
DB_DATABASE=todo
DB_USERNAME=todo
DB_PASSWORD=secret

# Optional: API URL for the frontend (Vite)
# If you change the backend port, update it here
# VITE_API_URL=http://localhost:8000
